import React from 'react';

const ProductsComponent = ({ title, value }) => {
  return (
    <div className="stats-container">
      <h2>{title}Aca va la tabla de todos los productos creados</h2>
      <p>{value} aguante taieerreeee</p>
    </div>
  );
};

export default ProductsComponent;